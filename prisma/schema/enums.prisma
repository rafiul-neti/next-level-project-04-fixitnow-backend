// users enums
enum Role {
  CUSTOMER
  TECHNICIAN
  ADMIN
}

enum UserStatus {
  BAN
  UNBAN
}

// bookings enums
enum BookingStatus {
  REQUESTED
  ACCEPTED
  DECLINED
  IN_PROGRESS
  COMPLETED
}

// technicinas enums
enum WeekendDays {
  FRI
  SAT
  SUN
}

// payments enums
enum PaymentStatus {
  PENDING
  FAILED
  PAID
  REQUESTED_REFUND
  REFUNDED
  CANCELLED
}

// address whereabout status
enum WhereAbout {
  HOME
  OFFICE
}

// payment provider enums
enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}
